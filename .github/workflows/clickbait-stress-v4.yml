name: Clickbait Frozen Rule Stress V4

on:
  workflow_dispatch:
  push:
    branches:
      - dataset-v3-realistic
    paths:
      - core/clickbait.py
      - data/v4_calibrated/**
      - data/dev_labels/v4_clickbait_stress_32.jsonl
      - experiments/benchmark_clickbait_stress_v4.py
      - .github/workflows/clickbait-stress-v4.yml

permissions:
  contents: write

jobs:
  stress-clickbait:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "torch>=2.4,<3" --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.44,<5" "scikit-learn>=1.5,<2" safetensors
      - name: Run frozen clickbait stress benchmark
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: |
          python experiments/benchmark_clickbait_stress_v4.py \
            --model intfloat/multilingual-e5-base \
            --output experiments/results/clickbait_stress_v4.json \
            --summary experiments/results/clickbait_stress_v4.md
      - name: Commit stress results
        run: |
          FILES="experiments/results/clickbait_stress_v4.json experiments/results/clickbait_stress_v4.md"
          if [ -z "$(git status --porcelain -- $FILES)" ]; then
            echo "Stress results already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add $FILES
          git commit -m "semantic-v3: record frozen clickbait stress results [skip ci]"
          git pull --rebase origin dataset-v3-realistic
          git push origin HEAD:dataset-v3-realistic
