name: V4 Hard Negative Patch Benchmark

on:
  workflow_dispatch:
  push:
    branches:
      - dataset-v3-realistic
    paths:
      - data/v4_calibrated/**
      - data/dev_labels/v4_contrastive_pairs_48.jsonl
      - data/v2_realistic/raw_posts.jsonl
      - experiments/benchmark_v4_hard_negative_patch.py
      - .github/workflows/v4-hard-negative-patch.yml

permissions:
  contents: write

jobs:
  hard-negative-benchmark:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "torch>=2.4,<3" --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.44,<5" "scikit-learn>=1.5,<2" safetensors
      - name: Run hard-negative patch benchmark
        env:
          TOKENIZERS_PARALLELISM: 'false'
        run: |
          python experiments/benchmark_v4_hard_negative_patch.py \
            --model intfloat/multilingual-e5-base \
            --output experiments/results/v4_hard_negative_patch.json \
            --summary experiments/results/v4_hard_negative_patch.md
      - name: Commit hard-negative results
        run: |
          FILES="experiments/results/v4_hard_negative_patch.json experiments/results/v4_hard_negative_patch.md"
          if [ -z "$(git status --porcelain -- $FILES)" ]; then
            echo "Hard-negative results already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add $FILES
          git commit -m "semantic-v2: record V4 hard-negative benchmark [skip ci]"
          git pull --rebase origin dataset-v3-realistic
          git push origin HEAD:dataset-v3-realistic
