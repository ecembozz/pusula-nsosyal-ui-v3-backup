name: E5 Base Error Analysis

on:
  workflow_dispatch:
  push:
    branches:
      - supervised-embedding-v1
    paths:
      - experiments/analyze_e5_base_errors.py
      - experiments/benchmark_encoder_sweep.py
      - experiments/benchmark_intent_balanced_corpus.py
      - experiments/benchmark_style_generalization.py
      - experiments/benchmark_supervised_embeddings.py
      - data/dev_labels/intent_balanced_training_v1.jsonl
      - data/gold_eval/draft_labels_annotator_a.jsonl
      - data/gold_eval/candidate_hard_cases.jsonl
      - data/gold_eval/candidate_news_cases.jsonl
      - .github/workflows/e5-base-error-analysis.yml

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install CPU dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install "torch>=2.4,<3" --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.44,<5" "scikit-learn>=1.5,<2" safetensors sentencepiece
      - name: Run E5-base error analysis
        env:
          TOKENIZERS_PARALLELISM: 'false'
          PYTHONUNBUFFERED: '1'
        run: |
          python experiments/analyze_e5_base_errors.py \
            --output experiments/results/e5_base_error_analysis.json \
            --summary experiments/results/e5_base_error_analysis.md
      - name: Commit diagnostic results
        run: |
          if [ -z "$(git status --porcelain -- experiments/results/e5_base_error_analysis.json experiments/results/e5_base_error_analysis.md)" ]; then
            echo "Results already up to date."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add experiments/results/e5_base_error_analysis.json experiments/results/e5_base_error_analysis.md
          git commit -m "semantic-v2: record E5-base error analysis [skip ci]"
          git push origin HEAD:supervised-embedding-v1
